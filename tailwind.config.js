// tailwind.config.js
module.exports = {
    darkMode: 'class', // this means dark mode only applies when the 'dark' class is on the <html> element
    theme: {
      extend: {},
    },
    plugins: [],
  };
  